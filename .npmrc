# puppeteer is a dev-only tool here (maps scrapers on the dev box); never download a Chrome build
# on install — on the phone it is useless (x86-64 / unsupported platform) and can abort bootstrap.
puppeteer_skip_download=true
